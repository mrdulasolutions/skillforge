# AI Tool Usage & Currency Disclosure

Append a filled copy of this block to every artifact this skill generates.
Skill Forge fills the `{{"{{"}}placeholders{{"}}"}}` from the audit log and run context.

---

**AI-assisted analysis — not professional advice.** This document was produced
by an AI agent (**skill `{{.Name}}`** v`{{"{{SKILL_VERSION}}"}}`, commit
`{{"{{SKILL_COMMIT_SHA7}}"}}`) running on `{{"{{MODEL_ID}}"}}` via
`{{"{{PLATFORM}}"}}` on `{{"{{ISO8601_DATETIME_UTC}}"}}`. User-provided inputs are
summarized here (input hash: `{{"{{INPUT_HASH_SHA256_FIRST12}}"}}`). All outputs
were generated by the AI and **require review by a qualified professional before
any reliance or action**. No professional relationship or legal opinion is
created by this output.

**Privacy-settings attestation.** The user attested that their AI platform opts
out of data collection and model training — tier: `{{"{{AI_PLATFORM_TIER}}"}}`.
Attested by `{{"{{ATTESTER}}"}}` at `{{"{{ATTESTATION_ISO8601}}"}}`.

**Data currency.** External sources consulted and their pull timestamps:
`{{"{{SOURCES_AND_TIMESTAMPS}}"}}`.

**Integrity log.** Prompt-injection / sanitization flags raised:
`{{"{{SANITIZE_FLAGS}}"}}` (see audit log for the chained record).

**Drift caveat.** Determinations older than 30 days should be re-run before
reliance.

**Human-in-the-loop.** The user explicitly confirmed inputs and the result at
`{{"{{HITL_CONFIRMATION_ISO8601}}"}}` before finalization.
